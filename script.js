import java.io.File;  // Import the File class
import java.io.IOException;  // Import the IOException class to handle errors
$("#g").click(function(){
  var fs = require("extras/ser.stop.txt");
  fs.writeFile("ser.js")
  close()
});
